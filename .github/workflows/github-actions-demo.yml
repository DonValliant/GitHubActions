name: Swift

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: /usr/bin/xcodebuild test -project "WorkingWithUIControls.xcodeproj" -scheme "Landmarks" clean build test
